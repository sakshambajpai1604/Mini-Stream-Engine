# Mini Stream Engine

Objective: Build filter/map/reduce over a list using Predicate/Function/BinaryOperator.
- Implement each operation taking the matching
- functional interface
- Chain them into a pipeline
- Compare output to java.util.stream equivalent

Validation: Output matches the JDK Stream equivalent exactly.
